# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass
from transformers import LlavaNextProcessor, LlavaNextForConditionalGeneration
from PIL import Image
import torch

class PyTorch_Transformers_LlavaNext_Image_Captioning(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        model_id = "thkim0305/llama3.2_3B_vl"

        self.processor = LlavaNextProcessor.from_pretrained(model_id)

        if multi_gpu and device == "cuda":
            self.model = LlavaNextForConditionalGeneration.from_pretrained(model_id, device_map="auto", torch_dtype="auto")
        else:
            self.model = LlavaNextForConditionalGeneration.from_pretrained(model_id, torch_dtype=torch.bfloat16)

        self.prompt = self.config.get('prompt', "<image>\nUSER: Provide a short, descriptive caption for this image.\nASSISTANT:")
        self.max_new_tokens = self.config.get('max_new_tokens', 75)

    def preprocess(self, input_images):
        images = [Image.open(image_path).convert('RGB') for image_path in input_images]
        prompts = [self.prompt] * len(images)
        model_input = self.processor(text=prompts, images=images, return_tensors="pt")
        return model_input

    def predict(self, model_input):
        return self.model.generate(**model_input, max_new_tokens=self.max_new_tokens)

    def postprocess(self, model_output):
        raw_preds = self.processor.batch_decode(model_output, skip_special_tokens=True)
        captions = []
        for pred in raw_preds:
            # The model output includes the prompt, so we split it to get only the generated part.
            parts = pred.split("ASSISTANT:")
            if len(parts) > 1:
                caption = parts[1].strip()
            else:
                caption = pred.strip() # Fallback in case the split token isn't present
            captions.append(caption)
        return captions
