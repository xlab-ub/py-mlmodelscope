# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from transformers import AutoProcessor, AutoModelForCausalLM
from PIL import Image
import torch

class PyTorch_Transformers_Qwen2_5_VL_Instruct_Quantized(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        model_id = "RedHatAI/Qwen2.5-VL-3B-Instruct-quantized.w8a8"

        self.processor = AutoProcessor.from_pretrained(model_id, trust_remote_code=True)

        if multi_gpu and device == "cuda":
            self.model = AutoModelForCausalLM.from_pretrained(
                model_id,
                torch_dtype="auto",
                device_map="auto",
                trust_remote_code=True
            )
        else:
            self.model = AutoModelForCausalLM.from_pretrained(
                model_id,
                trust_remote_code=True
            )

        self.prompt = self.config.get('prompt', "Describe this image in detail.")
        self.max_new_tokens = self.config.get('max_new_tokens', 64)

    def preprocess(self, input_images):
        raw_images = [Image.open(img_path).convert("RGB") for img_path in input_images]
        prompts = [self.prompt for _ in input_images]
        
        model_input = self.processor(text=prompts, images=raw_images, return_tensors="pt")
        return model_input

    def predict(self, model_input):
        model_input = {k: v.to(self.model.device) for k, v in model_input.items()}
        
        generated_ids = self.model.generate(
            **model_input,
            max_new_tokens=self.max_new_tokens
        )
        
        return generated_ids

    def postprocess(self, model_output):
        preds = self.processor.batch_decode(model_output, skip_special_tokens=True)
        return [pred.strip() for pred in preds]
