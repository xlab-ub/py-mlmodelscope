# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from transformers import CLIPProcessor, CLIPModel
from PIL import Image
import torch

class PyTorch_Transformers_CLIP_ViT_B_32_DataComp(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        model_id = "laion/CLIP-ViT-B-32-DataComp.XL-s13B-b90K"
        self.processor = CLIPProcessor.from_pretrained(model_id)

        if multi_gpu and device == "cuda":
            self.model = CLIPModel.from_pretrained(model_id, device_map="auto", torch_dtype="auto")
        else:
            self.model = CLIPModel.from_pretrained(model_id)
            self.model.to(device)

        self.model.eval()

        # Download and prepare ImageNet class labels for zero-shot classification
        features_file_url = "http://s3.amazonaws.com/store.carml.org/synsets/imagenet/synset.txt"
        self.features = self.features_download(features_file_url)
        # Format labels for CLIP using a standard template
        text_labels = [f"a photo of a {label}" for label in self.features]
        text_inputs = self.processor(text=text_labels, return_tensors="pt", padding=True)

        # Move text inputs to the correct device
        if not (multi_gpu and device == "cuda"):
            text_inputs = {k: v.to(device) for k, v in text_inputs.items()}

        # Pre-compute text features for efficiency
        with torch.no_grad():
            self.text_features = self.model.get_text_features(**text_inputs)

    def preprocess(self, input_images):
        processed_images = [
            Image.open(image_path).convert('RGB')
            for image_path in input_images
        ]
        # The processor handles resizing, normalization, and tensor conversion
        model_input_dict = self.processor(images=processed_images, return_tensors="pt")
        model_input = model_input_dict['pixel_values']
        return model_input

    def predict(self, model_input):
        with torch.no_grad():
            image_features = self.model.get_image_features(pixel_values=model_input)

        # Return a dictionary containing the image features and the pre-computed text features
        return {"image_features": image_features, "text_features": self.text_features}

    def postprocess(self, model_output):
        image_features = model_output["image_features"]
        text_features = model_output["text_features"]

        # Normalize features for cosine similarity calculation
        image_features = image_features / image_features.norm(p=2, dim=-1, keepdim=True)
        text_features = text_features / text_features.norm(p=2, dim=-1, keepdim=True)

        # Use the model's learned temperature parameter
        logit_scale = self.model.logit_scale.exp()
        # Calculate cosine similarity
        logits_per_image = logit_scale * image_features @ text_features.t()

        # Apply softmax to get probabilities
        probabilities = logits_per_image.softmax(dim=-1)

        return probabilities.tolist()
