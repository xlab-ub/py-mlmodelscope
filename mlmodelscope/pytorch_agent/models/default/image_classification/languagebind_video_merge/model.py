# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from transformers import AutoProcessor, AutoModel
from PIL import Image
import torch

class PyTorch_LanguageBind_Video_Merge(PyTorchAbstractClass):
    def __init__(self, config=None):
        super().__init__(config)
        model_id = "LanguageBind/LanguageBind_Video_merge"

        self.processor = AutoProcessor.from_pretrained(model_id, trust_remote_code=True)
        self.model = self.load_hf_model(AutoModel, model_id, trust_remote_code=True)
        self.model.eval()

        # Download ImageNet class labels for zero-shot classification
        features_file_url = "http://s3.amazonaws.com/store.carml.org/synsets/imagenet/synset.txt"
        self.features = self.features_download(features_file_url)

        # Pre-compute text embeddings for the class labels
        text_labels = [f"a photo of a {c}" for c in self.features]
        processed_text = self.processor(text=text_labels, padding=True, return_tensors="pt")
        processed_text = {k: v.to(self.device) for k, v in processed_text.items()}

        with torch.no_grad():
            text_outputs = self.model(**processed_text)
            self.text_embeds = text_outputs.text_embeds
            self.text_embeds /= self.text_embeds.norm(dim=-1, keepdim=True)

    def preprocess(self, input_images):
        images = [Image.open(image_path).convert("RGB") for image_path in input_images]
        model_input = self.processor(images=images, return_tensors="pt")
        return model_input

    def predict(self, model_input):
        image_outputs = self.model(**model_input)
        image_embeds = image_outputs.image_embeds
        image_embeds /= image_embeds.norm(dim=-1, keepdim=True)

        # Compute similarity between image and text embeddings
        logits = image_embeds @ self.text_embeds.t()
        return logits

    def postprocess(self, model_output):
        return torch.nn.functional.softmax(model_output, dim=1).tolist()
