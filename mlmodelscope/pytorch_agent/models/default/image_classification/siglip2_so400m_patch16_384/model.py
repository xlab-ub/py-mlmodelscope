# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from transformers import AutoProcessor, AutoModel
from PIL import Image
import torch

class PyTorch_Transformers_Siglip2_So400m_Patch16_384(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        model_id = "google/siglip2-so400m-patch16-384"
        self.processor = AutoProcessor.from_pretrained(model_id)

        if multi_gpu and device == "cuda":
            self.model = AutoModel.from_pretrained(model_id, device_map="auto", torch_dtype="auto")
        else:
            self.model = AutoModel.from_pretrained(model_id)

        self.model.eval()

    def preprocess(self, input_images):
        images = [Image.open(image_path).convert("RGB") for image_path in input_images]
        model_input = self.processor(images=images, return_tensors="pt")
        return model_input

    def predict(self, model_input):
        # This model is a vision-language model used for feature extraction, not direct classification.
        # The output will be image embeddings, not classification logits.
        with torch.no_grad():
            model_output = self.model.get_image_features(**model_input)
        return model_output

    def postprocess(self, model_output):
        # The model output is a tensor of image embeddings.
        # We convert the tensor to a list for a consistent output format.
        # No softmax is applied as the output is not logits.
        return model_output.tolist()
