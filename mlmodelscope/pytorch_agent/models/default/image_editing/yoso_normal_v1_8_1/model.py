# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass
import torch
from PIL import Image
import numpy as np

class PyTorch_Custom_StableNormal(PyTorchAbstractClass):
    def __init__(self, config=None):
        super().__init__(config)
        # As per the model card, this model is loaded from a GitHub repo via torch.hub
        # The specific version 'yoso-normal-v1-8-1' is passed as an argument.
        self.model = torch.hub.load("hugoycj/StableNormal", "StableNormal_turbo", trust_repo=True, yoso_version='yoso-normal-v1-8-1')

    def preprocess(self, input_images):
        images = [Image.open(img_path).convert('RGB') for img_path in input_images]
        return images

    def predict(self, model_input):
        # The model processes one image at a time
        output_images = []
        for image in model_input:
            with torch.no_grad():
                # The predictor is called directly on the PIL image
                normal_map = self.model(image)
            output_images.append(normal_map)
        return output_images

    def postprocess(self, model_output):
        # The model output is a list of PIL Images, which need to be converted to lists for JSON serialization
        return [np.array(img).tolist() for img in model_output]

    def to(self, device):
        self.device = device
        self.model.to(device)

    def eval(self):
        self.model.eval()
