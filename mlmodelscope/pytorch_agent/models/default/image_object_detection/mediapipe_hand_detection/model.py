# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from transformers import AutoImageProcessor, AutoModelForObjectDetection
from PIL import Image
import torch

class PyTorch_Transformers_MediaPipeHandDetection(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)
        model_id = "qualcomm/MediaPipe-Hand-Detection"

        self.processor = AutoImageProcessor.from_pretrained(model_id)
        if multi_gpu and device == "cuda":
            self.model = AutoModelForObjectDetection.from_pretrained(model_id, device_map="auto", torch_dtype="auto")
        else:
            self.model = AutoModelForObjectDetection.from_pretrained(model_id)

        self.image_sizes = []

    def preprocess(self, input_images):
        self.image_sizes = []
        images = []
        for image_path in input_images:
            img = Image.open(image_path).convert("RGB")
            self.image_sizes.append(img.size)
            images.append(img)
        model_input = self.processor(images=images, return_tensors="pt")
        return model_input

    def predict(self, model_input):
        return self.model(**model_input)

    def postprocess(self, model_output):
        target_sizes = torch.tensor(self.image_sizes)
        results = self.processor.post_process_object_detection(model_output, threshold=0.5, target_sizes=target_sizes)
        
        scores = []
        labels = []
        boxes = []
        for i in range(len(results)):
            scores.append(results[i]["scores"].tolist())
            labels.append(results[i]["labels"].tolist())
            boxes.append(results[i]["boxes"].tolist())

        return scores, labels, boxes
