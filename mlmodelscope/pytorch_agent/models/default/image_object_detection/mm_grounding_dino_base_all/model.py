# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from transformers import AutoProcessor, AutoModelForZeroShotObjectDetection
from PIL import Image

class PyTorch_Transformers_MMGroundingDinoBaseAll(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)
        model_id = "openmmlab-community/mm_grounding_dino_base_all"

        self.processor = AutoProcessor.from_pretrained(model_id)
        if multi_gpu and device == "cuda":
            self.model = AutoModelForZeroShotObjectDetection.from_pretrained(model_id, device_map="auto", torch_dtype="auto")
        else:
            self.model = AutoModelForZeroShotObjectDetection.from_pretrained(model_id)

        # Text prompts are required for this zero-shot model. They can be overridden in the config.
        self.text_prompts = self.config.pop("text_prompts", ["a cat", "a remote control"])
        self.target_sizes = []

    def preprocess(self, input_images):
        images = [Image.open(img_path).convert("RGB") for img_path in input_images]
        # Store original image sizes for post-processing, in (height, width) format
        self.target_sizes = [img.size[::-1] for img in images]

        # The model expects a list of lists for text prompts, one for each image in the batch.
        # We'll use the same set of prompts for every image in the batch.
        text_prompts_for_batch = [self.text_prompts] * len(images)

        model_input = self.processor(images=images, text=text_prompts_for_batch, return_tensors="pt")
        return model_input

    def predict(self, model_input):
        return self.model(**model_input)

    def postprocess(self, model_output):
        threshold = self.config.get("threshold", 0.4)
        results = self.processor.post_process_grounded_object_detection(
            model_output,
            threshold=threshold,
            target_sizes=self.target_sizes
        )
        # The output format is a list of results, one for each image.
        # We follow the standard of returning the results for the first image.
        result = results[0]
        scores = result["scores"].tolist()
        labels = result["labels"]  # Note: These are string labels, not integer IDs
        boxes = result["boxes"].tolist()
        return [scores], [labels], [boxes]
