# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from transformers import AutoProcessor, CsmForConditionalGeneration

class PyTorch_Transformers_Marvis_TTS_250m(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        model_id = "Marvis-AI/marvis-tts-250m-v0.1-transformers"

        self.processor = AutoProcessor.from_pretrained(model_id)
        
        if multi_gpu and device == "cuda":
            self.model = CsmForConditionalGeneration.from_pretrained(model_id, device_map="auto", torch_dtype="auto")
        else:
            self.model = CsmForConditionalGeneration.from_pretrained(model_id)

        self.features = {"sampling_rate": 24000}

    def preprocess(self, input_texts):
        return self.processor(text=input_texts, add_special_tokens=True, return_tensors="pt", padding=True)

    def predict(self, model_input):
        return self.model.generate(**model_input, output_audio=True)

    def postprocess(self, model_output):
        return model_output.cpu().numpy().squeeze(axis=1).tolist()
