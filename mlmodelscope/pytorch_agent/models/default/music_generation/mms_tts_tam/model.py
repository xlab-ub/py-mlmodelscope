# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from transformers import VitsModel, AutoTokenizer

class PyTorch_Transformers_MMS_TTS_TAM(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        self.processor = AutoTokenizer.from_pretrained("facebook/mms-tts-tam")
        
        if multi_gpu and device == "cuda":
            self.model = VitsModel.from_pretrained("facebook/mms-tts-tam", device_map="auto", torch_dtype="auto")
        else:
            self.model = VitsModel.from_pretrained("facebook/mms-tts-tam")

        self.features = {"sampling_rate": self.model.config.sampling_rate}

    def preprocess(self, input_texts):
        return self.processor(text=input_texts, return_tensors="pt", padding=True)

    def predict(self, model_input):
        return self.model(**model_input).waveform

    def postprocess(self, model_output):
        return model_output.cpu().numpy().tolist()
