# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass
from diffusers import AudioLDM2Pipeline
import torch

class PyTorch_Diffusers_AudioLDM2_Mustango(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        self.pipe = AudioLDM2Pipeline.from_pretrained("declare-lab/mustango", torch_dtype=torch.float16)
        self.pipe = self.pipe.to(device)

        self.num_inference_steps = self.config.get('num_inference_steps', 200)
        self.audio_length_in_s = self.config.get('audio_length_in_s', 10.0)

        self.features = {"sampling_rate": 16000}

    def preprocess(self, input_texts):
        return {"prompt": input_texts}

    def predict(self, model_input):
        return self.pipe(**model_input, num_inference_steps=self.num_inference_steps, audio_length_in_s=self.audio_length_in_s).audio

    def postprocess(self, model_output):
        return model_output.squeeze(axis=1).tolist()
