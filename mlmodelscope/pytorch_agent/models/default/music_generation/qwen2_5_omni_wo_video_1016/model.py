# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from transformers import AutoProcessor, AutoModelForCausalLM

class PyTorch_Transformers_Qwen2_Omni_Audio(PyTorchAbstractClass):
    def __init__(self, config=None):
        super().__init__(config)
        model_id = "boyuzhuGPT/qwen2_5_omni_wo_video_1016"
        # trust_remote_code is needed for custom Qwen models
        self.processor = AutoProcessor.from_pretrained(model_id, trust_remote_code=True)
        self.model = self.load_hf_model(AutoModelForCausalLM, model_id, trust_remote_code=True, torch_dtype="auto")

        # Default from generation_config.json is 4096, using a smaller default for typical use cases
        self.max_new_tokens = self.config.get('max_new_tokens', 1024)

        # NOTE: Sampling rate is not available in the model's config.
        # Using a common default for text-to-audio models (e.g., 16kHz).
        self.features = {"sampling_rate": 16000}

    def preprocess(self, input_texts):
        # The model expects a specific prompt format which is handled by the processor/tokenizer.
        return self.processor(text=input_texts, return_tensors="pt", padding=True)

    def predict(self, model_input):
        # Generate audio codes based on the tokenized text prompts.
        return self.model.generate(**model_input, max_new_tokens=self.max_new_tokens)

    def postprocess(self, model_output):
        # The model outputs audio codes (tokens), not a raw waveform.
        # This post-processing step returns the list of generated token IDs.
        # A separate vocoder would be needed to convert these codes into an audio waveform.
        # The output shape from generate is (batch_size, sequence_length).
        # The squeeze operation is kept for consistency with the reference example, but may not be ideal.
        return model_output.cpu().numpy().tolist()
