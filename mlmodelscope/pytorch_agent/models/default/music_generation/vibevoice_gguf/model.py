# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from transformers import AutoProcessor, BarkModel

class PyTorch_Transformers_VibeVoice_1_5B(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        # The requested model 'gguf-org/vibevoice-gguf' is a GGUF-quantized model which is not directly loadable by transformers.
        # Using the original base model 'microsoft/VibeVoice-1.5B' as identified in the model card.
        model_id = "microsoft/VibeVoice-1.5B"

        self.processor = AutoProcessor.from_pretrained(model_id)
        
        if multi_gpu and device == "cuda":
            self.model = BarkModel.from_pretrained(model_id, device_map="auto", torch_dtype="auto")
        else:
            self.model = BarkModel.from_pretrained(model_id)

        self.features = {"sampling_rate": self.model.generation_config.sample_rate}

    def preprocess(self, input_texts):
        return self.processor(text=input_texts, return_tensors="pt")

    def predict(self, model_input):
        return self.model.generate(**model_input)

    def postprocess(self, model_output):
        return model_output.cpu().numpy().squeeze(axis=1).tolist()
