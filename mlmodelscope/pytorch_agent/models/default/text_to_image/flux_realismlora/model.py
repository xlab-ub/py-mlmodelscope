# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

import torch
from diffusers import FluxPipeline

class PyTorch_Diffusers_Flux_RealismLora(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        base_model_path = "black-forest-labs/FLUX.1-dev"
        lora_model_path = "XLabs-AI/flux-RealismLora"

        if multi_gpu and device == "cuda":
            self.pipeline = FluxPipeline.from_pretrained(base_model_path, torch_dtype=torch.float16)
        else:
            self.pipeline = FluxPipeline.from_pretrained(base_model_path)
        
        self.pipeline.load_lora_weights(lora_model_path)
        self.num_inference_steps = self.config.get('num_inference_steps', 30)

    def preprocess(self, input_prompts):
        return input_prompts

    def predict(self, model_input):
        return self.pipeline(prompt=model_input, num_inference_steps=self.num_inference_steps).images

    def postprocess(self, model_output):
        import numpy as np
        return [np.array(img).tolist() for img in model_output]

    def to(self, device):
        self.device = device
        self.pipeline.to(device)

    def eval(self):
        pass
