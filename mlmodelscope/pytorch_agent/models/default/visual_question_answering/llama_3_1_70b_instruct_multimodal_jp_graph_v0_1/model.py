# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

import warnings
import torch
from transformers import LlavaNextProcessor, LlavaNextForConditionalGeneration
from PIL import Image
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

class PyTorch_Transformers_Llama_3_1_70B_Instruct_multimodal_JP_Graph_v0_1(PyTorchAbstractClass):
    def __init__(self, config=None):
        super().__init__(config)
        warnings.warn("Currently, this model may not support batched forward with multiple images of different sizes.")
        model_id = "r-g2-2024/Llama-3.1-70B-Instruct-multimodal-JP-Graph-v0.1"
        self.processor = LlavaNextProcessor.from_pretrained(model_id)
        self.model = self.load_hf_model(LlavaNextForConditionalGeneration, model_id, torch_dtype=torch.float16)

        if self.processor.tokenizer.pad_token is None:
            self.processor.tokenizer.pad_token = self.processor.tokenizer.eos_token

        self.max_new_tokens = self.config.get('max_new_tokens', 100)

    def preprocess(self, input_image_and_questions):
        images = [Image.open(item[0]).convert('RGB') for item in input_image_and_questions]
        questions = [item[1] for item in input_image_and_questions]
        prompts = [f"[INST] <image>\n{q} [/INST]" for q in questions]
        return self.processor(text=prompts, images=images, return_tensors="pt", padding=True)

    def predict(self, model_input):
        return self.model.generate(**model_input, max_new_tokens=self.max_new_tokens, pad_token_id=self.processor.tokenizer.eos_token_id)

    def postprocess(self, model_output):
        decoded_outputs = self.processor.batch_decode(model_output, skip_special_tokens=True)
        return [output.split('[/INST]')[1].strip() if '[/INST]' in output else output for output in decoded_outputs]
