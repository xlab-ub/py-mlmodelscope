# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

import torch
from PIL import Image
from transformers import AutoProcessor, AutoModelForCausalLM
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

class PyTorch_Transformers_MiniCPM_V_4(PyTorchAbstractClass):
    def __init__(self, config=None):
        super().__init__(config)
        # The user requested a GGUF model, which is not supported by transformers.
        # We are using the original PyTorch base model 'openbmb/MiniCPM-V-4' instead.
        model_id = "openbmb/MiniCPM-V-4"
        self.processor = AutoProcessor.from_pretrained(model_id, trust_remote_code=True)
        self.model = self.load_hf_model(
            AutoModelForCausalLM,
            model_id,
            trust_remote_code=True,
            torch_dtype=torch.float16
        )

        if self.processor.tokenizer.pad_token is None:
            self.processor.tokenizer.pad_token = self.processor.tokenizer.eos_token

        self.max_new_tokens = self.config.get('max_new_tokens', 100)
        self.assistant_prompt_tag = "<|start_header_id|>assistant<|end_header_id|>\n"

    def preprocess(self, input_image_and_questions):
        images = [Image.open(item[0]).convert('RGB') for item in input_image_and_questions]
        questions = [item[1] for item in input_image_and_questions]

        prompts = []
        for question in questions:
            msgs = [{'role': 'user', 'content': f'<image>\n{question}'}]
            prompt = self.processor.tokenizer.apply_chat_template(msgs, tokenize=False, add_generation_prompt=True)
            prompts.append(prompt)

        return self.processor(text=prompts, images=images, return_tensors="pt", padding=True)

    def predict(self, model_input):
        return self.model.generate(
            **model_input,
            max_new_tokens=self.max_new_tokens,
            pad_token_id=self.processor.tokenizer.pad_token_id
        )

    def postprocess(self, model_output):
        decoded_outputs = self.processor.batch_decode(model_output, skip_special_tokens=True)
        return [output.split(self.assistant_prompt_tag)[-1].strip() for output in decoded_outputs]
