# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

import torch
from transformers import Qwen2_5_VLForConditionalGeneration, AutoProcessor
from PIL import Image

class PyTorch_Transformers_Qwen2_5_VL_7B_Instruct(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        model_id = "Qwen/Qwen2.5-VL-7B-Instruct"
        self.processor = AutoProcessor.from_pretrained(model_id)
        
        if multi_gpu and device == "cuda":
            self.model = Qwen2_5_VLForConditionalGeneration.from_pretrained(model_id, torch_dtype="auto", device_map="auto")
        else:
            self.model = Qwen2_5_VLForConditionalGeneration.from_pretrained(model_id, torch_dtype="auto")

        self.max_new_tokens = self.config.get('max_new_tokens', 128)

    def preprocess(self, input_image_and_questions):
        images = [Image.open(item[0]).convert("RGB") for item in input_image_and_questions]
        
        all_messages = [
            [
                {
                    "role": "user",
                    "content": [
                        {"type": "image"},
                        {"type": "text", "text": item[1]},
                    ],
                }
            ]
            for item in input_image_and_questions
        ]

        texts = [self.processor.apply_chat_template(msg, tokenize=False, add_generation_prompt=True) for msg in all_messages]

        return self.processor(text=texts, images=images, return_tensors="pt", padding=True)

    def predict(self, model_input):
        return self.model.generate(**model_input, max_new_tokens=self.max_new_tokens)

    def postprocess(self, model_output):
        decoded_outputs = self.processor.batch_decode(model_output, skip_special_tokens=True)
        # The model's output includes the prompt. We split the output string at the assistant's turn to get the answer.
        return [output.split('assistant\n')[-1].strip() for output in decoded_outputs]
