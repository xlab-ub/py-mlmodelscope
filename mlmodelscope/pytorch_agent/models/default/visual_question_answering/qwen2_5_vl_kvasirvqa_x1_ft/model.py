# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

import torch
from transformers import AutoProcessor, Qwen2ForConditionalGeneration
from PIL import Image
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

class PyTorch_Transformers_SimulaMet_Qwen2_5_VL_KvasirVQA_x1_ft(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        model_id = "SimulaMet/Qwen2.5-VL-KvasirVQA-x1-ft"
        # The processor must be loaded from the base model to ensure correct chat templates and tokenization
        processor_id = "Qwen/Qwen2.5-VL-7B-Instruct"

        self.processor = AutoProcessor.from_pretrained(processor_id, trust_remote_code=True)

        # Use bfloat16 as specified in the model's training configuration for best performance
        torch_dtype = torch.bfloat16

        if multi_gpu and device == "cuda":
            self.model = Qwen2ForConditionalGeneration.from_pretrained(model_id, device_map="auto", torch_dtype=torch_dtype, trust_remote_code=True)
        else:
            self.model = Qwen2ForConditionalGeneration.from_pretrained(model_id, torch_dtype=torch_dtype, trust_remote_code=True)

        self.max_new_tokens = self.config.get('max_new_tokens', 128)
        # Store input length to properly decode only the generated tokens in postprocessing
        self.input_text_len = 0

    def preprocess(self, input_image_and_questions):
        images = [Image.open(item[0]).convert('RGB') for item in input_image_and_questions]
        questions = [item[1] for item in input_image_and_questions]

        prompts = []
        for question in questions:
            messages = [
                {
                    "role": "user",
                    "content": [
                        {"type": "image"},
                        {"type": "text", "text": question}
                    ]
                }
            ]
            prompt = self.processor.tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
            prompts.append(prompt)

        model_inputs = self.processor(text=prompts, images=images, return_tensors="pt", padding=True)
        return model_inputs

    def predict(self, model_input):
        # Store the length of the input prompt to isolate the generated response in postprocessing
        self.input_text_len = model_input.input_ids.shape[1]
        return self.model.generate(
            **model_input.to(self.model.device),
            pad_token_id=self.processor.tokenizer.eos_token_id,
            max_new_tokens=self.max_new_tokens
        )

    def postprocess(self, model_output):
        # Slice the output to get only the newly generated tokens, excluding the input prompt
        output_ids = model_output[:, self.input_text_len:]
        decoded_answers = self.processor.batch_decode(output_ids, skip_special_tokens=True, clean_up_tokenization_spaces=False)
        return [ans.strip() for ans in decoded_answers]
