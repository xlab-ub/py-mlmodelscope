# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

import torch
import warnings
from transformers import AutoModelForCausalLM, AutoProcessor
from PIL import Image

class PyTorch_Transformers_Qwen3_VL_30B_A3B_Instruct_MLX_8bit(PyTorchAbstractClass):
    def __init__(self, config=None):
        warnings.warn("Currently, this model may not support batched forward with multiple images of different sizes efficiently.")
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        # The requested model is an MLX quantized version. We use the PyTorch base model for compatibility.
        model_id = "Qwen/Qwen3-VL-30B-A3B-Instruct"
        self.processor = AutoProcessor.from_pretrained(model_id, trust_remote_code=True)
        
        if multi_gpu and device == "cuda":
            self.model = AutoModelForCausalLM.from_pretrained(model_id, device_map="auto", torch_dtype="auto", trust_remote_code=True)
        else:
            self.model = AutoModelForCausalLM.from_pretrained(model_id, torch_dtype="auto", trust_remote_code=True)

        self.max_new_tokens = self.config.get('max_new_tokens', 100)
        self.input_token_len = 0

    def preprocess(self, input_image_and_questions):
        images = [Image.open(item[0]).convert('RGB') for item in input_image_and_questions]
        questions = [item[1] for item in input_image_and_questions]

        prompts = []
        for question in questions:
            messages = [
                {
                    "role": "user",
                    "content": [
                        {"type": "image"},
                        {"type": "text", "value": question}
                    ]
                }
            ]
            prompt = self.processor.tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
            prompts.append(prompt)

        model_input = self.processor(text=prompts, images=images, return_tensors="pt")
        self.input_token_len = model_input['input_ids'].shape[1]
        return model_input

    def predict(self, model_input):
        eos_token_id = self.processor.tokenizer.eos_token_id
        return self.model.generate(**model_input, max_new_tokens=self.max_new_tokens, eos_token_id=eos_token_id)

    def postprocess(self, model_output):
        # Slice the output tokens to get only the generated part, excluding the prompt
        output_tokens = model_output[:, self.input_token_len:]
        return self.processor.batch_decode(output_tokens, skip_special_tokens=True)
