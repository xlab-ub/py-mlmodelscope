# Generated by automation script
from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass

from mlmodelscope.pytorch_agent.models.pytorch_abc import PyTorchAbstractClass
from transformers import Qwen3VLForConditionalGeneration, AutoProcessor
from PIL import Image
import torch

class PyTorch_Transformers_Qwen3_VL_32B_Instruct(PyTorchAbstractClass):
    def __init__(self, config=None):
        self.config = config if config else dict()
        device = self.config.pop("_device", "cpu")
        multi_gpu = self.config.pop("_multi_gpu", False)

        model_id = "Qwen/Qwen3-VL-32B-Instruct"
        self.processor = AutoProcessor.from_pretrained(model_id)

        if multi_gpu and device == "cuda":
            self.model = Qwen3VLForConditionalGeneration.from_pretrained(model_id, dtype="auto", device_map="auto")
        else:
            self.model = Qwen3VLForConditionalGeneration.from_pretrained(model_id, dtype="auto")

        self.max_new_tokens = self.config.get('max_new_tokens', 128)

    def preprocess(self, input_image_and_questions):
        images = [Image.open(item[0]).convert("RGB") for item in input_image_and_questions]
        
        prompts = []
        for _, question in input_image_and_questions:
            messages = [
                {
                    "role": "user",
                    "content": [
                        {"type": "image"},
                        {"type": "text", "text": question},
                    ]
                }
            ]
            prompt = self.processor.tokenizer.apply_chat_template(
                messages,
                tokenize=False,
                add_generation_prompt=True
            )
            prompts.append(prompt)
            
        model_inputs = self.processor(
            text=prompts,
            images=images,
            return_tensors="pt",
            padding=True
        )
        return model_inputs

    def predict(self, model_input):
        generated_ids = self.model.generate(**model_input, max_new_tokens=self.max_new_tokens, pad_token_id=self.processor.tokenizer.eos_token_id)
        return {"generated_ids": generated_ids, "input_ids": model_input["input_ids"]}

    def postprocess(self, model_output):
        generated_ids = model_output["generated_ids"]
        input_ids = model_output["input_ids"]
        
        generated_ids_trimmed = [out_ids[len(in_ids):] for in_ids, out_ids in zip(input_ids, generated_ids)]
        
        return self.processor.batch_decode(generated_ids_trimmed, skip_special_tokens=True, clean_up_tokenization_spaces=False)
